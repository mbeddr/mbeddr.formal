-- var section
-------------------------------

MODULE module_with_many_vars()
VAR
  -- very first comment line
  flag_var : boolean; -- another comment on the same line as var decl
  -- second comment line
  enum_var : {idle, start, run};
  range_var : 0..3;
  -- third comment line
  enum_var_with_comments : {
    -- idle
	idle, 
	-- start
	start, 
	stop --stopped
	}